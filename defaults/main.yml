---
# Set the version of Node.js to install (e.g. "0.10", "0.12")
nodejs_version: "0.12"

# Install forever, which is useful for running nodeJS apps
nodejs_forever: true

# Define a list of global packages to be installed with npm
nodejs_npm_global_packages: []
#  # Install a specific version of a package.
#  - name: gulp
#    version: 3.9.0
#  # Install the latest stable release of a package.
#  - name: node-sass
